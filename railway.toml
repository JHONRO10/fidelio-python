[deploy]
startCommand = "python -m uvicorn webhook:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
# v1.0.3 — fixes encoding + Supabase grants
